{% set slides = [] %}
{% for item in items %}
  {% set slides = slides|merge([item.content]) %}
{% endfor %}
{% include "@circle/partials/slidershow.html.twig" with {
  "slides": slides,
  "has_buttons": true,
  "has_pagination": false
}%}